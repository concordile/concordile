name: conc

services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: concdb
      POSTGRES_PASSWORD: concpass
      POSTGRES_USER: concuser
    ports:
      - '5432:5432'
