spring:
  datasource:
    url: jdbc:h2:mem:openapi;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  flyway:
    enabled: false
  docker:
    compose:
      enabled: false

server:
  port: 28080

management:
  server:
    port: 29090
